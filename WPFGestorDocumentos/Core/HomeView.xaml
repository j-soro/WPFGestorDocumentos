<UserControl x:Class="WPFGestorDocumentos.Core.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFGestorDocumentos.Core"
             xmlns:viewmodels="clr-namespace:WPFGestorDocumentos.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:UserCollectionsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="800">
    <Viewbox StretchDirection="Both"
             Stretch="Uniform"
             MaxWidth="1600"
             MaxHeight="900"
             VerticalAlignment="Top">
        <StackPanel>
            <TextBlock Text="Collections"
                   Foreground="White"
                   FontSize="28"
                   HorizontalAlignment="Left"
                   Margin="0 0 0 20"/>

            <ItemsControl ItemsSource="{Binding superColeccion}">
                

            </ItemsControl>

        </StackPanel>
    </Viewbox>
</UserControl>
